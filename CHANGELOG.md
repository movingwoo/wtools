# Changelog

이 프로젝트는 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/) 형식과
[Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [Unreleased]

### 품질 및 운영

- 자체 구현 코드를 `js/lib`·`js/workers`로 분리하는 기준, 지연 로드·서비스 워커 캐시 계층,
  검증 단계와 안정 릴리즈 체크리스트를 개발 가이드로 명문화
- 도구 모듈 80 KiB, 자체 구현·Worker 모듈 128 KiB 단일 파일 상한을 정적 검사에 추가
- 압축 코덱과 파일 해시 Worker를 도구 모듈의 Blob 소스 문자열에서 독립된 정적 Worker 진입점으로 분리하고, 계층 역참조와 신규 인라인 Worker를 정적 검사에서 차단
- EXIF 파싱, JPEG·PNG 메타데이터 제거, ICO 조립 로직을 `media.js`에서 지연 로드되는 순수 이미지 포맷 모듈로 분리

## [1.3.1] - 2026-08-24

1.3.0 이후 발생한 GitHub Actions 런타임 폐기 경고를 제거하고 Pages 배포와
도구 화면 안내를 정리한 패치 릴리즈입니다.

### 변경됨

- 모든 도구 상단에 반복되던 브라우저 내 처리 및 외부 라이브러리 로드 안내 제거

### 품질 및 운영

- GitHub Actions의 checkout·setup-node·cache를 Node.js 24 기반 최신 메이저로 올려 폐기된 Node.js 20 런타임 경고 제거
- GitHub Pages를 레거시 브랜치 빌드에서 Node.js 24 기반 공식 Actions 정적 배포 워크플로로 전환

## [1.3.0] - 2026-08-24

인증서와 대용량 데이터 변환 워크플로를 추가하고 전반적인 보안·품질·호환성을
강화한 마이너 릴리즈입니다.

### 추가됨

- RSA/EC 개인키로 Subject와 DNS/IP/이메일 SAN을 담은 PKCS#10 CSR을 생성·다운로드하고, CSR 공개키·서명 알고리즘·자체 서명과 약한 키를 검사하는 도구 추가
- 개인키·공개키·CSR·X.509 인증서의 정규화된 SPKI와 SHA-256 지문을 비교해 같은 키 쌍인지 확인하는 도구 추가
- 순서가 섞인 인증서 PEM을 정렬하고 사용자 지정 신뢰 앵커, TLS DNS/IP 호스트명·serverAuth, 유효기간, CA·pathLen·DNS Name Constraints, 상위·자체 서명과 로컬 CRL을 검사하며 명시적 온라인 AIA·서명 검증된 OCSP·CRL 확인까지 제공하는 도구 추가
- JSON Lines/NDJSON과 JSON 배열·CSV·YAML을 상호 변환하고 네 포맷 파일 입력, NDJSON·JSON 배열·CSV 청크 파싱, YAML 제한 파싱, 진행률·취소, 지원 브라우저 디스크 직접 저장과 128 MiB 호환 다운로드를 제공하는 도구 추가

### 보안

- ZIP/Tar 해제 전에 안전한 경로, 중복 이름, 헤더·CRC 체크섬, 항목 수, 단일·총 해제 크기와 압축률을 검사하고 제한 초과 입력을 중단
- 이미지 헤더의 크기·총 픽셀 예산을 디코딩 전에 확인하고 일괄 변환을 한 장씩 처리하며, 취소·부분 실패 요약과 실패 항목 재시도 제공
- 파일 해시를 2 MiB 청크 Worker 방식으로 바꾸고 알고리즘별 진행률·취소, 전체 복사와 체크섬 목록 다운로드 추가
- RSA 새 키를 2048비트 이상으로 제한하고 SHA-1 서명 생성과 확인 없는 DES/3DES/ECB 새 암호화를 차단
- PEM/JWK 전달에 일회성 추가 동의를 요구하고 모든 전달 형식을 중앙에서 검증
- 인증서 온라인 검사는 사용자가 전송 대상을 확인한 뒤 명시적으로 실행하도록 분리하고, 자격 증명 없이 4 MiB 이하 HTTP(S) 응답만 받아 AIA 발급자·OCSP 응답자·CRL 서명과 기간을 검증

### 변경됨

- 130개 도구의 검색 메타데이터를 자동 생성 manifest로 분리하고 구현은 도구 진입 시 지연 로드; 모듈 오류는 `MOD001`로 격리하고 재시도 지원
- 서비스 워커 캐시 이름을 앱 셸 SHA-256 지문으로 자동 갱신하고 실패 응답에 데이터 없는 진단 코드·복구 안내 추가
- JSON Schema 지원 범위를 공식 Draft 4/6/7/2019-09/2020-12 핵심 벡터로 명시하고 외부 `$ref`와 미지원 최신 키워드를 명시적으로 거부
- IDN에 UTS #46/IDNA 길이·금지 문자·NFC 검사와 혼합 스크립트 피싱 경고 추가
- 모든 도구 화면에 브라우저 내 처리 여부와 최초 외부 라이브러리 로드 조건을 일관되게 표시

### 품질 및 운영

- 공통 파일 개수·단일 크기·총합 예산, 접근 가능한 `그래도 처리` 확인과 수동 비동기 실행기의 상태·잠금·취소·재시도·이탈 정리 추가
- axe-core WCAG 검사, 검색 tree/combobox 관계, 320px·200% 확대·키보드·감소 모션·고대비 및 라이트/다크 시각 계약 회귀 테스트 추가
- Chromium 112, Firefox 111, WebKit 16.4 정기 호환성 잡과 최소 기준 이후 API 정적 검사, WebCrypto·Worker·Canvas/ImageBitmap·WASM·클립보드·카메라 대체 안내 테스트 추가
- PWA manifest 안정 ID, Chromium 설치 가능성·아이콘·시작 URL 검사와 nightly 운영 요약 추가
- 초기 로컬 JavaScript 140 KiB 예산과 홈에서 도구 구현을 요청하지 않는 성능 회귀 테스트 추가
- Playwright를 1.62.1로 올려 브라우저 다운로드 검증 보안 공지를 해소하고 axe-core를 포함한 테스트 의존성 등록부, 월간 감사와 업데이트 체크리스트 추가

### 테스트

- USTAR/ZIP을 Python 표준 구현과 교차 검증하고 UTF-8·디렉터리·중복·긴 경로, 경로 순회·CRC 오류·압축 폭탄 모의 입력을 추가
- 파일 예산 경계·다중 총합·취소, 손상 이미지·픽셀 상한, 민감 값·같은 도구·다중 입력·포맷 옵션 전달을 자동 검증
- OpenSSL로 실행 중 생성한 CSR·서버·중간·루트 인증서·CRL·OCSP 응답으로 생성·파싱·키 일치·신뢰 앵커·호스트명·체인 정렬·폐기 상태를 교차 검증하고 서명 변조·관계없는 체인을 탐지
- JSON/NDJSON/CSV/YAML 변환, BOM·CRLF·빈 줄·줄 번호 오류, 512 KiB 청크 경계를 넘는 NDJSON·JSON 배열, 따옴표 내 줄바꿈 CSV, YAML 파일과 Blob/디스크 직접 저장·취소를 자동 검증

## [1.2.2] - 2026-08-21

실행 의존성 무결성과 오프라인 복구를 강화하고, 정적 배포 산출물 및
운영 보안 검증을 추가한 패치 릴리즈입니다.

### 추가됨

- GitHub Release 발행 시 플랫폼 독립적인 정적 호스팅 ZIP과 SHA-256 체크섬을 자동으로 첨부
- 릴리즈 ZIP을 다시 풀어 HTTP로 제공한 뒤 앱 셸 전체를 검사하는 배포 산출물 검증 추가
- 운영 CSP/XFO, Referrer-Policy, Permissions-Policy의 누락·중복·잘못된 값을 검사하는 의존성 없는 스크립트와 nightly 회귀 검사 추가
- 제3자 코드의 URL, SHA-384, 라이선스, 사용처와 최신 검토일을 공유하는 단일 등록부 추가

### 보안

- SRI를 적용할 수 없던 외부 ESM 9개와 하위 import/WASM 2개를 검토한 로컬 자산으로 고정하고, 등록부를 우회한 외부 실행 URL을 정적 검사에서 차단
- 서비스 워커가 등록된 외부 script/CSS와 로컬 제3자 ESM/WASM의 SHA-384를 확인한 뒤에만 캐시하고, 불일치 응답과 캐시를 폐기하도록 강화

### 변경됨

- Brotli, Zstandard, Bzip2, LZ4, TOML, GIF, OpenPGP 도구가 실행 시 외부 ESM을 가져오지 않고 릴리즈에 포함된 검토본을 사용
- EFF 짧은 단어 목록의 라이선스 안내를 원출처의 현재 저작권 정책과 같은 CC BY 4.0으로 통일

### 수정됨

- 이미지 포맷 변환기의 편집·재인코딩 안내가 기본 화면에서 별도 note 두 개로 표시되던 구성을 하나로 통합
- 서비스 워커가 앱 셸에 없는 navigation의 404 응답을 받으면 캐시된 앱 화면으로 복구하도록 보완

### 테스트

- 서비스 워커 설치·제어권 획득, 구버전 캐시 삭제, 검증된 외부/로컬 자산의 오프라인 사용, 변조 응답·캐시 폐기와 한국어 오류를 Chromium에서 검증
- 오프라인 도구 URL 새로고침·navigation fallback과 대기 Worker 적용 후 한 번만 새로고침되는 업데이트 흐름을 Chromium에서 검증
- 운영 사이트에서 보안 응답 헤더의 단일 적용과 데스크톱·모바일 카메라, 클립보드, Argon2 WASM, 샌드박스 Markdown 미리보기 및 Cloudflare 인라인 부트스트랩 차단을 확인
- 제3자 자산 등록부의 버전·라이선스·SHA-384·사용처, 로컬 파일과 앱 셸 포함 여부, 우회 외부 코드 URL을 정적 검사에 추가

## [1.2.1] - 2026-08-20

파일 체크섬 검증 흐름과 공통 파일 입력을 다듬고, 브라우저 테스트 환경을
재현 가능한 컨테이너로 고정한 패치 릴리즈입니다.

### 추가됨

- 파일 해시에 기대 체크섬 직접 입력과 GNU/BSD 체크섬 목록 가져오기를 추가하고, 여러 파일의 일치·불일치·누락·추가 상태를 일괄 표시

### 수정됨

- 공통 파일 입력에서 브라우저 기본 파일 선택 버튼이 드래그·드롭 영역과 중복 노출되던 문제를 수정하고, 키보드·보조 기술용 파일 찾아보기 동작과 안내는 유지

### 테스트

- Node.js 22 정책과 잠금 파일, `npm ci`, Playwright 수집 검사를 로컬·PR·nightly에 일치시켜 브라우저 테스트 설치와 기동을 재현 가능하게 고정
- Playwright 공식 컨테이너를 버전과 digest로 고정하고 Chromium 전체 및 Firefox·WebKit 스모크 테스트를 순차 실행해, 실행 중 브라우저·Linux 의존성 설치 없이 크로스 브라우저 검사를 유지
- 테스트 종료 시 진행 중인 CDN 캐시 라우트를 정리해 외부 라이브러리 요청의 종료 오류가 테스트 실패로 번지지 않도록 보완
- 원시 체크섬과 GNU/BSD 형식 판독, 다중 파일 검증 및 잘못된 입력에 대한 Chromium 회귀 테스트 추가

## [1.2.0] - 2026-08-12

도구 간 작업 흐름을 연결하고 JWT, Cron, QR/바코드, 압축, 토큰 및 이미지 도구의
실사용 기능을 확장한 마이너 릴리즈입니다.

### 추가됨

- Base64 디코딩, JWT payload, JSON, HMAC 등 호환되는 결과를 다른 도구의 원하는 입력 칸으로 보내는 메모리 내 전달 기능
- JWT의 PS256/384/512 및 ES256/384/512 생성·검증, 시간 클레임 clock skew와 `iss`·`aud`·`sub` 기대값 검증
- IANA 시간대와 DST, 월말, 윤년 및 일/요일 OR 의미를 반영한 Cron 다음 실행 시각 5회 계산
- QR/바코드 실시간 카메라 스캔, 카메라 전환·일시정지와 EAN·Code 128·Data Matrix 인식
- Brotli 압축과 Zstandard 압축·해제, 압축 레벨 및 Worker 기반 파일 처리
- EFF 단어 기반 패스프레이즈, 혼동 문자 제외, 문자군별 최소 개수와 엔트로피 안내
- 이미지 변환기의 회전, 좌우·상하 반전, 자르기와 EXIF 방향 정규화
- JSON Schema 샘플 생성의 로컬 `$ref`, `required`, 조합 스키마, 배열 및 패턴 지원
- 텍스트 Diff의 공백 차이 무시, 통합 diff 미리보기와 다운로드
- 모든 파일 입력의 공통 끌어놓기 및 클립보드 파일 붙여넣기 UI

### 변경됨

- JWT 서명 검증과 클레임 검증을 분리하고 `alg=none`, 알고리즘 불일치 및 약한 HS 시크릿 경고를 강화
- QR 또는 바코드에서 인식한 URL은 자동으로 열지 않고 사용자가 내용을 확인한 뒤 이동하도록 유지
- 이미지 일괄 변환에서 공통 설정과 파일별 설정의 우선순위를 명확히 하고 미리보기와 결과를 일치시킴
- Bzip2는 브라우저 번들 비용과 유지보수성을 검토한 결과 압축을 추가하지 않고 Worker 기반 해제를 유지

### 보안 및 개인정보

- 도구 간 전달값을 URL이나 영구 저장소에 남기지 않고 현재 탭 메모리에서 일회성으로 처리
- 사용자 입력을 외부로 전송하는 DNS over HTTPS 도구에 전송 대상·항목, 개인정보 주의사항과 CORS 의존성을 실행 전에 표시
- QR/바코드 카메라 스트림을 라우트 이동이나 화면 종료 시 즉시 정리

### 테스트

- 공개 JWT/JWS 및 압축 테스트 벡터, 시간·DST·윤년·입력 오류, 파일 왕복과 이미지 픽셀 결과 회귀 테스트 추가
- 정적 검사에서 126개 도구와 15개 테스트 모듈의 등록 일치를 확인
- Chromium 전체 Playwright 테스트 595개 통과

## [1.1.0] - 2026-08-11

이미지를 문자로 표현하는 새 변환 도구와 로컬 테스트 환경 개선을 포함한 마이너 릴리즈입니다.

### 추가됨

- 이미지의 픽셀 밝기를 문자 밀도에 매핑하는 이미지 아스키아트 변환기 추가
- 가로 문자 수, 기본·상세·블록 문자셋, 밝기 반전 및 원본 색상 유지 옵션 제공
- 변환 결과의 텍스트 복사와 TXT 다운로드, 색상 유지 결과의 PNG 다운로드 지원
- 로컬 브라우저 테스트가 CI와 동일한 Node.js 22를 사용하도록 `.node-version` 추가

### 변경됨

- 기존 FIGlet 기반 도구 이름을 `ASCII 텍스트 배너 생성기`로 변경해 이미지 아스키아트 변환기와 용도를 명확히 구분
- 로컬 Playwright 실행에 필요한 Node.js 버전과 상위 메이저 사용 시 발생하는 문제를 문서에 명시

### 테스트

- 이미지 명암 매핑, 문자셋, 밝기 반전, 문자 수 검증 및 TXT/PNG 다운로드 테스트 추가
- 이미지·문자열 관련 Chromium 테스트와 정적 등록 검증 통과

## [1.0.1] - 2026-08-09

기존 도구의 정확성과 입력 검증을 보완한 패치 릴리즈입니다.

### 변경됨

- AES-GCM을 기본 권장 방식으로 추가하고 키 크기, 랜덤 IV, salt, 인증 태그를 실제 암호화와 자체 포함 Base64/Hex 왕복에 반영
- AES의 잘못된 키·IV·암호문 오류를 구체화하고 CBC/CTR/ECB 및 DES/3DES/Blowfish의 레거시·비인증 특성을 안내
- Crontab 설명기에 월·요일 이름과 범위·목록·간격 조합을 반영하고, 일과 요일을 함께 제한할 때 일반적인 cron의 OR 의미를 명시
- CSV 변환기에 구분자와 헤더 유무 옵션을 추가하고, 닫히지 않은 따옴표의 줄 번호 및 잘못된 따옴표 구문을 검증
- CSV의 비어 있거나 중복된 헤더와 헤더보다 긴 행을 자동 보정해 모든 열 데이터를 보존
- 이미지 변환기의 GIF 단일 프레임, PNG 포함 SVG, 원본 포맷 재인코딩 동작을 명확히 표시하고 JPEG/GIF/BMP 배경색 선택을 추가

### 테스트

- AES 모드·키 크기·출력 형식별 왕복과 공개 테스트 벡터 추가
- Crontab 일·요일 OR 의미 및 이름·범위·목록·간격 회귀 테스트 추가
- CSV CRLF, 셀 내부 줄바꿈, 따옴표 이스케이프, 빈 마지막 셀과 헤더 보존 테스트 추가
- 이미지 출력 포맷 안내와 투명 픽셀 배경색 합성 테스트 추가

## [1.0.0] - 2026-08-07

첫 정식 릴리즈. 빌드 과정 없는 순수 정적 사이트로, 12개 카테고리에 걸친
개발자 유틸리티를 제공합니다.

### 카테고리

- 인코딩 / 디코딩 (Base64/32/58/85, URL, JWT, 모스 부호, 진법 변환 등)
- 데이터 포맷 변환 (JSON/YAML/XML/CSV/TOML/ENV 상호 변환, JSONPath, JSON Schema 등)
- 코드 포맷팅 / 개발 유틸리티 (포맷터, Diff, 정규식, Crontab, Docker, cURL↔fetch 등)
- 문자열 / 텍스트 유틸리티 (대소문자 변환, 유니코드 분석, 한글 도구 등)
- 해싱 (MD/SHA/SHA3/BLAKE 계열, HMAC, 파일 체크섬 등)
- 암호화 / 복호화 (AES/DES/Blowfish, RSA, ECDSA/Ed25519, PGP, TOTP/HOTP 등)
- 공개키 / 인증서 (X.509, ASN.1, PEM↔Hex, SSH 키 등)
- 네트워크 (서브넷 계산, CIDR, MAC, DNS over HTTPS, User-Agent 파서 등)
- 날짜 / 시간 (Unix 타임스탬프, Filetime, UTC↔로컬, 스톱워치 등)
- 이미지 / 미디어 / QR (QR 생성/리더, 이미지 포맷 변환, EXIF, 파비콘 생성 등)
- 수학 / 논리 / 랜덤 (비트 연산, 수식 계산기, 단위 변환, UUID/ULID/NanoID 등)
- 압축 / 아카이브 (Gzip, Zip, Tar, LZMA, LZ4 등)

### 공통 기능

- 검색창 입력 형식(JWT/JSON/URL/Base64/해시) 자동 감지 및 도구 추천
- 서비스 워커 기반 앱 셸 오프라인 지원 및 PWA 설치
- 라이트/다크 테마, 반응형 UI, 접근성 알림

전체 기능 목록은 [FEATURES.md](FEATURES.md)를 참고하세요.
