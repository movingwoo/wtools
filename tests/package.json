{
  "name": "wtools-browser-tests",
  "private": true,
  "type": "module",
  "description": "CI 전용 브라우저 스모크 테스트. 사이트 자체는 의존성 없이 정적 호스팅됩니다.",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "playwright test",
    "test:collect": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "axe-core": "4.10.3"
  }
}
